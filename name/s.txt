#include <stdio.h>
#include <time.h>
#include <stdlib.h>
#include<string.h>

int ecrire(){
    int a;
    char nom[20];

    FILE *fp;

    scanf("%d",&a);
    scanf("%s",&nom);

    strcat(nom,".txt");
    fp=fopen(nom,"w");

   // fp = fopen("a.txt","w");
    fprintf(fp,"%d",a);
    fclose(fp);
}

int lire(){
    int d;
    char nom[20];

    FILE *fp;

    scanf("%s",&nom);

    strcat(nom,".txt");

    fp=fopen(nom,"r");

    //fp = fopen("a.txt","r");
    fscanf(fp,"%d",&d);
    printf("%d",d);
    fclose(fp);
    printf("\n");
}

int main(){
    int a,b=0;

    while (b!=1){
    printf("Ecrire 0\nLire 1\n:");

    scanf("%d",&a);
        switch (a) {
            case 0:
                ecrire();
                break;
            case 1:
                lire();
                break;

            case 20:
                b=1;
                break;
        }
    }

}



