#include <stdio.h>
#include <time.h>
#include <stdlib.h>

int ecrire(){
    int a;

    FILE *fp;

    scanf("%d",&a);


    fp = fopen("a.txt","w");
    fprintf(fp,"%d",a);
    fclose(fp);

}

int lire(){
    int d;

    FILE *fp;

    fp = fopen("a.txt","r");
    fscanf(fp, "%d",&d);
    printf("%d",d);
    fclose(fp);
}

int main(){
    int a,b=0;

    while (b!=1){
    printf("Ecrire 0\nLire 1\n:");

    scanf("%d",&a);
        switch (a) {
            case 0:
                ecrire();
                break;
            case 1:
                lire();
                break;

            case 20:
                b=1;
                break;
        }
    }

}

